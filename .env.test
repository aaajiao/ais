# Test-only dummy Supabase config so vitest can build the client at import time
# (createClient throws on an empty URL: "supabaseUrl is required").
# The URL matches the MSW handler base in src/test/mocks/handlers.ts so requests
# stay intercepted. Real values come from Vercel env in production.
VITE_SUPABASE_URL=https://test.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
