# aaajiao 工作室作品库存管理系统 - 技术方案 v3.0

*文档版本: v3.0*  
*创建日期: 2025-01-23*

---

## 1. 项目概述

### 1.1 背景

工作室当前使用 Excel 管理作品库存，存在以下问题：
- 多人协作困难，版本混乱
- 与网站 (eventstructure.com) 数据分离，需要手动同步
- 难以追踪版本状态变更历史
- 无法快速查询和生成报表

### 1.2 系统定位

**核心定位：版本生命周期管理 + 仓储管理**

```
eventstructure.com（网站）
  └── 作品展示（公开）：完整描述、高清图片、艺术家陈述

本系统（内部）
  └── 版本/仓储管理：库存状态、位置追踪、装箱照片、销售记录
```

**不做的事**：
- 不复制网站的作品展示功能
- 不存储作品描述（通过链接跳转网站查看）
- 不做公开访问

### 1.3 核心场景

| 场景 | 用户行为 | 系统响应 |
|------|----------|----------|
| 作品售出 | "Guard 1/3 卖了，5万美金，买家是某基金会" | 更新状态、记录销售信息 |
| 送画廊寄售 | "把这件送去 Tabula Rasa 寄售" | 更新状态、记录寄售开始日期 |
| 作品入库 | "这件放到柏林仓库 A区3号架" + 上传装箱照片 | 更新位置、保存附件 |
| 查询统计 | "今年卖了多少？Tabula Rasa 现在有我哪些作品？" | 查询并汇总 |
| 生成报表 | "给画廊一份寄售清单" | 筛选、格式化、导出 |
| 数据导入 | "导入这个 MD 文件" | 解析、对比、增量更新 |

---

## 2. 系统架构

### 2.1 技术栈

```
┌─────────────────────────────────────────────────────────────┐
│                     前端 (Vite + React)                     │
│  ├── @ai-sdk/react (useChat)                               │
│  ├── @supabase/supabase-js                                 │
│  ├── TailwindCSS + shadcn/ui                               │
│  └── 部署: Vercel (静态托管)                                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  后端 (Vercel Functions)                    │
│  ├── ai (Vercel AI SDK - streamText, tools)                │
│  ├── 多模型支持:                                            │
│  │   ├── @ai-sdk/anthropic (Claude)                        │
│  │   ├── @ai-sdk/openai (GPT)                              │
│  │   ├── @ai-sdk/google (Gemini)                           │
│  │   └── @ai-sdk/groq (Llama - 快速)                       │
│  └── @supabase/supabase-js                                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     数据层 (Supabase)                        │
│  ├── PostgreSQL (artworks, editions, edition_files, ...)   │
│  ├── Storage (thumbnails, edition-files)                   │
│  └── Auth (可选，如需多用户)                                 │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 多模型支持

Vercel AI SDK 支持多种 AI 模型，可根据任务类型和成本需求灵活切换。

**支持的模型**：

| 提供商 | 模型 | 包 | 特点 |
|--------|------|-----|------|
| Anthropic | Claude Sonnet 4, Haiku | `@ai-sdk/anthropic` | 工具调用准确 |
| OpenAI | GPT-4o, GPT-4-turbo | `@ai-sdk/openai` | 速度快，生态成熟 |
| Google | Gemini 2.0 Flash/Pro | `@ai-sdk/google` | 超便宜 |
| Groq | Llama 3, Mixtral | `@ai-sdk/groq` | 超快响应 |
| Mistral | Mistral Large | `@ai-sdk/mistral` | 欧洲数据合规 |
| 本地 | Ollama, LM Studio | `ollama-ai-provider` | 完全免费，隐私 |

**模型选择策略**：

| 任务类型 | 推荐模型 | 理由 |
|----------|----------|------|
| 复杂操作（状态变更、导入） | Claude Sonnet | 工具调用准确，理解复杂指令 |
| 简单查询（搜索、统计） | Gemini Flash / Groq | 便宜/快速 |
| 报表生成 | GPT-4o | 格式化输出稳定 |
| 开发/测试 | Ollama (本地) | 免费，无网络依赖 |

**实现方式**：

```typescript
// lib/models.ts
import { anthropic } from "@ai-sdk/anthropic";
import { openai } from "@ai-sdk/openai";
import { google } from "@ai-sdk/google";
import { groq } from "@ai-sdk/groq";

export const models = {
  // 复杂任务 - 准确优先
  complex: anthropic("claude-sonnet-4-20250514"),
  
  // 简单任务 - 成本优先
  simple: google("gemini-2.0-flash"),
  
  // 快速响应
  fast: groq("llama-3.3-70b-versatile"),
  
  // 备选
  fallback: openai("gpt-4o"),
};

// 根据任务类型选择模型
export function selectModel(taskType: "complex" | "simple" | "fast") {
  return models[taskType] || models.complex;
}
```

```typescript
// api/chat.ts
import { selectModel } from "../lib/models";

export default async function handler(req: Request) {
  const { messages, taskType = "complex" } = await req.json();
  
  const result = streamText({
    model: selectModel(taskType),
    messages,
    tools: { ... },
  });
  
  return result.toDataStreamResponse();
}
```

**配置管理**：

```typescript
// .env.local
ANTHROPIC_API_KEY=sk-ant-xxx      # Claude
OPENAI_API_KEY=sk-xxx             # GPT
GOOGLE_GENERATIVE_AI_API_KEY=xxx  # Gemini
GROQ_API_KEY=gsk_xxx              # Groq

# 默认模型（可通过环境变量切换）
DEFAULT_MODEL=claude-sonnet
```

**用户可选**（可选功能）：

可以在设置页面让用户选择偏好模型：

```
┌─────────────────────────────────────────────────────────────┐
│  设置 > AI 模型                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  默认模型:                                                  │
│  ○ Claude Sonnet (推荐，最准确)                             │
│  ○ GPT-4o (快速)                                           │
│  ○ Gemini Flash (最便宜)                                   │
│  ○ Groq Llama (最快响应)                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 项目结构

```
aaajiao-inventory/
├── api/                          # Vercel Functions (后端)
│   ├── chat.ts                   # AI 对话 + 工具调用
│   ├── auth/
│   │   ├── callback.ts           # OAuth 回调
│   │   └── verify.ts             # 邮箱白名单验证
│   ├── gallery/
│   │   └── [token].ts            # 画廊门户 API
│   ├── import/
│   │   ├── md.ts                 # MD 文件解析导入
│   │   └── url.ts                # URL 抓取导入
│   ├── export/
│   │   ├── pdf.ts                # PDF 导出（带嵌入图片）
│   │   ├── md.ts                 # MD 导出（带图片链接）
│   │   ├── excel.ts              # Excel 导出
│   │   ├── csv.ts                # CSV 导出
│   │   └── json.ts               # JSON 完整备份
│   └── files/
│       └── upload.ts             # 文件上传
│
├── src/                          # Vite + React (前端)
│   ├── main.tsx
│   ├── App.tsx
│   ├── components/
│   │   ├── ui/                   # shadcn/ui 组件
│   │   ├── auth/
│   │   │   ├── LoginPage.tsx     # 登录页
│   │   │   └── ProtectedRoute.tsx
│   │   ├── chat/
│   │   │   ├── ChatPanel.tsx     # 对话面板
│   │   │   ├── Message.tsx
│   │   │   ├── EditableConfirmCard.tsx  # 可编辑确认卡片
│   │   │   └── ConfirmDialog.tsx # 简单确认弹窗
│   │   ├── artworks/
│   │   │   ├── ArtworkList.tsx
│   │   │   ├── ArtworkCard.tsx
│   │   │   └── ArtworkDetail.tsx
│   │   ├── editions/
│   │   │   ├── EditionList.tsx
│   │   │   ├── EditionCard.tsx
│   │   │   ├── EditionDetail.tsx
│   │   │   └── HistoryTimeline.tsx
│   │   ├── files/
│   │   │   ├── FileUpload.tsx
│   │   │   ├── FileList.tsx
│   │   │   └── ImagePicker.tsx   # 缩略图选择
│   │   ├── gallery/
│   │   │   ├── GalleryPortal.tsx # 画廊门户页面
│   │   │   └── GalleryLinkManager.tsx # 链接管理
│   │   ├── export/
│   │   │   ├── ExportDialog.tsx  # 导出对话框
│   │   │   └── ExportPreview.tsx # 导出预览
│   │   └── import/
│   │       ├── MDImport.tsx
│   │       └── URLImport.tsx
│   ├── lib/
│   │   ├── supabase.ts
│   │   ├── models.ts             # AI 模型配置
│   │   ├── auth.ts               # 认证工具函数
│   │   ├── offlineCache.ts       # IndexedDB 离线缓存
│   │   ├── syncQueue.ts          # 离线同步队列
│   │   ├── types.ts
│   │   └── utils.ts
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useTheme.ts           # 主题切换
│   │   ├── useNetworkStatus.ts   # 网络状态
│   │   └── ...
│   └── pages/
│       ├── Dashboard.tsx
│       ├── Artworks.tsx
│       ├── Editions.tsx
│       ├── Import.tsx
│       ├── Settings.tsx          # 设置（含画廊链接管理）
│       └── gallery/
│           └── [token].tsx       # 画廊门户（公开页面）
│
├── public/
│   ├── manifest.json             # PWA manifest
│   ├── sw.js                     # Service Worker
│   ├── offline.html              # 离线回退页面
│   └── icons/
│       ├── icon-192.png
│       ├── icon-512.png
│       ├── icon-192-maskable.png
│       ├── apple-touch-icon.png
│       └── apple-touch-icon-dark.png
│
├── vite.config.ts
├── vercel.json
├── tailwind.config.js
├── package.json
└── .env.local
```

### 2.4 部署配置

```json
// vercel.json
{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:3000'  // vercel dev
    }
  }
})
```

---

## 3. 交互设计：Agent-first

### 3.1 设计理念

**读用 UI，写用对话**

| 操作类型 | 交互方式 | 理由 |
|----------|----------|------|
| 浏览列表 | 传统 UI | 视觉扫描比问答快 |
| 查看详情 | 传统 UI | 结构化信息展示更清晰 |
| 状态变更 | 对话 | 涉及多字段，自然语言更高效 |
| 数据导入 | 对话 | "导入这个文件"比点击流程更直接 |
| 批量操作 | 对话 | "把超期寄售的都标记一下" |
| 生成报表 | 对话 | "给画廊一份清单" |

### 3.2 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  Logo   [作品] [版本] [导入]              🔍 [对话]         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────┐ ┌───────────────────┐ │
│  │                                 │ │                   │ │
│  │                                 │ │   对话面板        │ │
│  │        主内容区                  │ │                   │ │
│  │     (列表/详情/导入)             │ │  💬 Guard 1/3    │ │
│  │                                 │ │     卖了5万美金   │ │
│  │                                 │ │                   │ │
│  │                                 │ │  🤖 好的，我来   │ │
│  │                                 │ │     记录...      │ │
│  │                                 │ │                   │ │
│  │                                 │ │  [输入框]        │ │
│  └─────────────────────────────────┘ └───────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

对话面板可折叠/展开，也可通过快捷键 `Cmd+K` 唤起。

### 3.3 对话流程示例

**示例 1: 记录销售**

```
用户: Guard 1/3 卖了

Agent: 好的，我来帮你记录这笔销售。需要补充：
       • 成交价格？
       • 买家是谁？
       • 成交日期？

用户: 5万美金，柏林某基金会，上周五

Agent: 明白了，确认一下：
       
       ┌─────────────────────────────┐
       │ 作品: Guard, I… - 1/3       │
       │ 状态: 在库 → 已售           │
       │ 价格: $50,000 USD           │
       │ 买家: 柏林某基金会           │
       │ 日期: 2025-01-17            │
       └─────────────────────────────┘
       
       确认更新吗？ [确认] [修改]

用户: 确认

Agent: ✅ 已更新。
       要为这个版本分配唯一编号吗？
       检测到你的编号格式是 AAJ-YYYY-NNN，建议: AAJ-2025-001

用户: 好

Agent: ✅ 已分配编号 AAJ-2025-001
```

**示例 2: 数据导入**

```
用户: [上传 MD 文件] 导入这个

Agent: 解析完成，发现 150 件作品：
       
       📥 新增 3 件
       🔄 有更新 5 件（材料、尺寸等字段变化）
       ✓ 无变化 142 件
       
       要导入新增和更新的作品吗？ [全部导入] [只导入新增] [查看详情]

用户: 全部导入

Agent: 为 3 件新作品选择缩略图：
       
       Guard, I…
       [📷1] [📷2] [📷3] [📷4] ← 点击选择
       
       ...

用户: [选择完成]

Agent: ✅ 导入完成
       • 新增 3 件
       • 更新 5 件
       
       要为新作品配置版本吗？
```

**示例 3: 批量查询**

```
用户: 哪些寄售超过6个月了？

Agent: 找到 4 件超期寄售：
       
       │ 作品          │ 版本 │ 画廊        │ 寄售时长 │
       │ Deep Web      │ 2/3  │ Tabula Rasa │ 8个月   │
       │ February      │ 1/5  │ 某画廊A     │ 7个月   │
       │ ...           │      │             │         │
       
       要发送跟进提醒吗？或者批量更新状态？

用户: 给我导出一份清单

Agent: ✅ 已生成清单 [下载 CSV]
```

### 3.4 可编辑确认卡片

**问题**：AI 解析后如果有错误，用户重新用自然语言描述很麻烦。

**解决方案**：确认卡片支持直接编辑，用户可以快速修改 AI 识别的内容。

**三种编辑模式**：

| 模式 | 触发方式 | 适用场景 |
|------|----------|----------|
| **查看模式** | 默认 | AI 解析正确，直接确认 |
| **内联编辑** | 点击单个字段 | 只需改一两个字段 |
| **完整编辑** | 点击 ✏️ 按钮 | 需要改多个字段 |

**交互流程**：

```
用户: "Guard 1/3 卖了，5万欧，给柏林某基金会，上周五"

┌─────────────────────────────────────────────────────────────┐
│  确认更新                                           [✏️]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  作品    Guard, I… - 1/3                                   │
│  状态    在库 → 已售                                        │
│  价格    €50,000                              ← 点击可改    │
│  买家    柏林某基金会                          ← 点击可改    │
│  日期    2025-01-17                           ← 点击可改    │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [取消]                                      [确认更新]     │
└─────────────────────────────────────────────────────────────┘

--- 场景 A: 价格错了，点击价格字段 ---

┌─────────────────────────────────────────────────────────────┐
│  确认更新                                           [✏️]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  作品    Guard, I… - 1/3                                   │
│  状态    在库 → 已售                                        │
│  价格    [45000        ] [EUR ▼]              [✓] [✗]      │
│          ↑ 内联编辑模式                                     │
│  买家    柏林某基金会                                        │
│  日期    2025-01-17                                         │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [取消]                                      [确认更新]     │
└─────────────────────────────────────────────────────────────┘

--- 场景 B: 多个字段要改，点击 ✏️ ---

┌─────────────────────────────────────────────────────────────┐
│  编辑更新                                          [完成]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  作品    Guard, I… - 1/3                       (不可改)    │
│                                                             │
│  状态    [已售              ▼]                              │
│                                                             │
│  价格    [45000        ]    [EUR ▼]                         │
│                                                             │
│  买家    [柏林艺术基金会_______________________]            │
│                                                             │
│  日期    [2025-01-17        📅]                             │
│                                                             │
│  备注    [__________________________________]  (可选)       │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [取消]                                      [确认更新]     │
└─────────────────────────────────────────────────────────────┘
```

**组件设计**：

```tsx
// components/EditableConfirmCard.tsx
interface ConfirmCardProps {
  title: string;
  fields: {
    key: string;
    label: string;
    value: any;
    editable: boolean;
    type: 'text' | 'number' | 'select' | 'date';
    options?: string[];  // for select
  }[];
  onConfirm: (values: Record<string, any>) => void;
  onCancel: () => void;
}

function EditableConfirmCard({ title, fields, onConfirm, onCancel }: ConfirmCardProps) {
  const [mode, setMode] = useState<'view' | 'edit'>('view');
  const [editingField, setEditingField] = useState<string | null>(null);
  const [values, setValues] = useState(/* initial from fields */);

  return (
    <div className="bg-white dark:bg-slate-800 rounded-xl border ...">
      <div className="flex justify-between p-4 border-b">
        <span>{mode === 'view' ? title : `编辑${title}`}</span>
        <button onClick={() => setMode(mode === 'view' ? 'edit' : 'view')}>
          {mode === 'view' ? <EditIcon /> : '完成'}
        </button>
      </div>
      
      <div className="p-4 space-y-3">
        {fields.map(field => (
          <FieldRow
            key={field.key}
            field={field}
            value={values[field.key]}
            mode={mode}
            isEditing={editingField === field.key}
            onStartEdit={() => setEditingField(field.key)}
            onEndEdit={() => setEditingField(null)}
            onChange={(v) => setValues({...values, [field.key]: v})}
          />
        ))}
      </div>
      
      <div className="flex gap-3 p-4 border-t">
        <button onClick={onCancel}>取消</button>
        <button onClick={() => onConfirm(values)}>确认更新</button>
      </div>
    </div>
  );
}
```

**移动端适配**：

```
移动端内联编辑：
┌─────────────────────────────┐
│ 价格  €50,000          [✏️] │  ← 点击 ✏️ 展开编辑
├─────────────────────────────┤
│ [45000        ] [EUR ▼]     │
│           [取消] [确认]     │
└─────────────────────────────┘

移动端完整编辑：
→ 滑出底部 Sheet 或全屏表单
```

**设计原则**：

1. **默认查看模式** — 大多数情况 AI 解析正确
2. **点击即编辑** — 最少操作步骤
3. **保留上下文** — 编辑时仍能看到其他字段
4. **支持键盘** — Tab 切换字段，Enter 确认

### 3.5 Agent 工具定义

```typescript
// api/chat.ts
const tools = {
  // 查询类
  search_artworks: {
    description: "搜索作品",
    parameters: z.object({
      query: z.string().optional(),
      type: z.string().optional(),
      year: z.string().optional(),
    }),
  },
  
  search_editions: {
    description: "搜索版本",
    parameters: z.object({
      artworkId: z.string().optional(),
      status: z.enum(["in_studio", "at_gallery", "at_museum", "sold", ...]).optional(),
      locationId: z.string().optional(),
      locationQuery: z.string().optional(), // AI 智能匹配位置
      consignmentOverdue: z.boolean().optional(),
    }),
  },
  
  get_statistics: {
    description: "获取统计数据",
    parameters: z.object({
      type: z.enum(["sales", "inventory", "consignment"]),
      period: z.string().optional(),
    }),
  },
  
  // 位置管理（动态学习）
  search_locations: {
    description: "搜索位置（支持模糊匹配和别名）",
    parameters: z.object({
      query: z.string(),
      type: z.enum(["studio", "gallery", "museum", "other"]).optional(),
    }),
  },
  
  create_location: {
    description: "创建新位置",
    parameters: z.object({
      name: z.string(),
      type: z.enum(["studio", "gallery", "museum", "other"]),
      aliases: z.array(z.string()).optional(),
      city: z.string().optional(),
      country: z.string().optional(),
    }),
    requiresConfirmation: true,
  },
  
  add_location_alias: {
    description: "为位置添加别名",
    parameters: z.object({
      locationId: z.string(),
      alias: z.string(),
    }),
  },

  // 写入类（需要确认）
  update_edition: {
    description: "更新版本状态",
    parameters: z.object({
      editionId: z.string(),
      status: z.string().optional(),
      locationId: z.string().optional(),       // 引用 locations 表
      locationQuery: z.string().optional(),    // AI 智能匹配/创建位置
      storageDetail: z.string().optional(),
      salePrice: z.number().optional(),
      saleCurrency: z.enum(["USD", "EUR", "CNY", "GBP", "CHF", "HKD", "JPY"]).optional(),
      buyerName: z.string().optional(),
      saleDate: z.string().optional(),
      inventoryNumber: z.string().optional(),
      notes: z.string().optional(),
    }),
    requiresConfirmation: true,  // 需要用户确认
  },
  
  create_editions: {
    description: "为作品创建版本",
    parameters: z.object({
      artworkId: z.string(),
      editionTotal: z.number(),
      apTotal: z.number().optional(),
      isUnique: z.boolean().optional(),
    }),
    requiresConfirmation: true,
  },
  
  batch_update: {
    description: "批量更新版本",
    parameters: z.object({
      editionIds: z.array(z.string()),
      updates: z.object({
        status: z.string().optional(),
        locationId: z.string().optional(),
        notes: z.string().optional(),
      }),
    }),
    requiresConfirmation: true,
  },

  // 导入导出
  import_artworks: {
    description: "导入作品数据",
    parameters: z.object({
      source: z.enum(["md", "url"]),
      data: z.any(),
    }),
    requiresConfirmation: true,
  },
  
  export_artworks: {
    description: "导出作品数据",
    parameters: z.object({
      scope: z.enum(["single", "selected", "filtered", "all"]),
      artworkIds: z.array(z.string()).optional(),
      format: z.enum(["pdf", "md", "excel", "csv", "json"]),
      filters: z.object({}).optional(),
    }),
  },
  
  generate_report: {
    description: "生成报表（库存、寄售、销售等）",
    parameters: z.object({
      type: z.enum(["inventory", "consignment", "sales", "history"]),
      filters: z.object({
        gallery: z.string().optional(),
        status: z.string().optional(),
        dateFrom: z.string().optional(),
        dateTo: z.string().optional(),
      }).optional(),
      format: z.enum(["pdf", "excel", "csv"]).default("excel"),
    }),
  },

  // 编号
  suggest_inventory_number: {
    description: "建议下一个库存编号",
    parameters: z.object({}),
  },
};
```

---

## 4. 设计风格与响应式

参考 vocab-tracker 的设计风格，保持一致的视觉语言。

### 4.1 配色方案

**深色模式（默认）**

| 元素 | 颜色 | Tailwind |
|------|------|----------|
| 背景 | #0f172a | `bg-slate-900` |
| 卡片背景 | #1e293b | `bg-slate-800` |
| 边框 | #334155 | `border-slate-700` |
| 主文字 | #f1f5f9 | `text-slate-100` |
| 次要文字 | #94a3b8 | `text-slate-400` |
| 强调色 | #f97316 | `text-orange-500` |
| 成功 | #22c55e | `text-green-500` |
| 警告 | #eab308 | `text-yellow-500` |
| 危险 | #ef4444 | `text-red-500` |

**亮色模式**

| 元素 | 颜色 | Tailwind |
|------|------|----------|
| 背景 | #f8fafc | `bg-slate-50` |
| 卡片背景 | #ffffff | `bg-white` |
| 边框 | #e2e8f0 | `border-slate-200` |
| 主文字 | #0f172a | `text-slate-900` |
| 次要文字 | #64748b | `text-slate-500` |

### 4.2 主题切换

```tsx
// hooks/useTheme.ts
export function useTheme() {
  const [theme, setTheme] = useState<'dark' | 'light'>(() => {
    // 读取本地存储，默认深色
    return localStorage.getItem('theme') as 'dark' | 'light' || 'dark';
  });

  useEffect(() => {
    document.documentElement.classList.toggle('dark', theme === 'dark');
    localStorage.setItem('theme', theme);
  }, [theme]);

  const toggle = () => setTheme(t => t === 'dark' ? 'light' : 'dark');
  
  return { theme, toggle };
}
```

```tsx
// 切换按钮
<button onClick={toggle}>
  {theme === 'dark' ? <SunIcon /> : <MoonIcon />}
</button>
```

### 4.3 骨架屏加载

数据加载时显示骨架屏，而不是简单的 "Loading..."

```tsx
// components/Skeleton.tsx
function SkeletonBox({ className }: { className: string }) {
  return (
    <div
      className={`animate-pulse bg-slate-200 dark:bg-slate-700 rounded ${className}`}
    />
  );
}

// 作品卡片骨架
export function ArtworkCardSkeleton() {
  return (
    <div className="bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl p-4">
      <div className="flex gap-4">
        <SkeletonBox className="w-20 h-20 rounded-lg" />  {/* 缩略图 */}
        <div className="flex-1">
          <SkeletonBox className="w-3/4 h-5 mb-2" />      {/* 标题 */}
          <SkeletonBox className="w-1/2 h-4 mb-2" />      {/* 年份 */}
          <SkeletonBox className="w-1/3 h-4" />            {/* 版本 */}
        </div>
      </div>
    </div>
  );
}

// 统计卡片骨架
export function StatsCardSkeleton() {
  return (
    <div className="bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl p-3">
      <SkeletonBox className="w-10 h-7 mb-1" />
      <SkeletonBox className="w-8 h-3" />
    </div>
  );
}

// 页面骨架
export function PageSkeleton() {
  return (
    <div className="p-4">
      {/* 统计区域 */}
      <div className="grid grid-cols-2 sm:grid-cols-4 gap-3 mb-6">
        <StatsCardSkeleton />
        <StatsCardSkeleton />
        <StatsCardSkeleton />
        <StatsCardSkeleton />
      </div>
      {/* 搜索框 */}
      <SkeletonBox className="w-full h-10 rounded-xl mb-6" />
      {/* 卡片列表 */}
      <ArtworkCardSkeleton />
      <ArtworkCardSkeleton />
      <ArtworkCardSkeleton />
    </div>
  );
}
```

**加载效果**：

```
┌─────────────────────────────────────────────────────────────┐
│  ░░░░░░  ░░░░░░  ░░░░░░  ░░░░░░                             │  ← 统计卡片骨架
├─────────────────────────────────────────────────────────────┤
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░                   │  ← 搜索框骨架
├─────────────────────────────────────────────────────────────┤
│  ┌──────┐  ░░░░░░░░░░░░░░░░░░░░░                            │
│  │ ░░░░ │  ░░░░░░░░░░░░░                                    │  ← 卡片骨架
│  └──────┘  ░░░░░░░░░                                        │
│                                                             │
│  ┌──────┐  ░░░░░░░░░░░░░░░░░░░░░                            │
│  │ ░░░░ │  ░░░░░░░░░░░░░                                    │
│  └──────┘  ░░░░░░░░░                                        │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 响应式布局

**断点定义**：

| 断点 | 宽度 | 设备 |
|------|------|------|
| `sm` | ≥640px | 大手机横屏 |
| `md` | ≥768px | iPad 竖屏 |
| `lg` | ≥1024px | iPad 横屏、小桌面 |
| `xl` | ≥1280px | 桌面 |

**布局策略**：

| 场景 | 移动端 (< 768px) | 桌面端 (≥ 768px) |
|------|------------------|------------------|
| 统计卡片 | 2×2 网格 | 1×4 横排 |
| 作品列表 | 单列卡片 | 表格或多列 |
| 详情页 | 全屏页面 | 右侧面板 |
| 对话面板 | 浮动按钮 → 全屏 | 侧边栏常驻 |
| 导航 | 底部 Tab 栏 | 顶部导航 |

### 4.5 移动端布局

**底部导航栏**（推荐方案）：

```
┌─────────────────────────────────────┐
│   🏠      📦      💬      ⚙️       │
│  首页    作品    对话    设置       │
└─────────────────────────────────────┘
```

| Tab | 功能 |
|-----|------|
| 🏠 首页 | 统计概览、快捷入口 |
| 📦 作品 | 作品/版本列表 |
| 💬 对话 | AI 对话面板 |
| ⚙️ 设置 | 主题、导出、账户 |

**首页**：

```
┌─────────────────────────────────────┐
│  📦 作品管理              ☀️        │  ← 顶部栏
├─────────────────────────────────────┤
│  ┌────────┐ ┌────────┐              │
│  │ 150    │ │ 45     │              │
│  │ 总作品  │ │ 在库   │              │  ← 2×2 统计
│  ├────────┤ ├────────┤              │
│  │ 12     │ │ 3      │              │
│  │ 寄售中  │ │ 已售   │              │
│  └────────┘ └────────┘              │
├─────────────────────────────────────┤
│  快捷操作                           │
│  ┌─────────┐ ┌─────────┐           │
│  │ 📥 导入  │ │ 📤 导出  │           │
│  └─────────┘ └─────────┘           │
│  ┌─────────┐ ┌─────────┐           │
│  │ 🔍 搜索  │ │ ➕ 新增  │           │
│  └─────────┘ └─────────┘           │
├─────────────────────────────────────┤
│  最近更新                           │
│  ├── Guard, I… - 1/3  状态变更     │
│  ├── Deep Web - 2/5   新增附件     │
│  └── February - AP 1  已售出       │
├─────────────────────────────────────┤
│   🏠      📦      💬      ⚙️       │
│  首页    作品    对话    设置       │
└─────────────────────────────────────┘
```

**作品列表**：

```
┌─────────────────────────────────────┐
│  作品            🔍                  │
├─────────────────────────────────────┤
│  [全部] [在库] [寄售] [已售]  →     │  ← 可滚动标签
├─────────────────────────────────────┤
│  📅 2025年1月                       │
│  ┌─────────────────────────────────┐│
│  │ [📷] Guard, I…                  ││
│  │      2024 · 3+2AP               ││
│  │      🟢2  🟡1  🔴0              ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │ [📷] Deep Web                   ││
│  │      2024 · 5+2AP               ││
│  │      🟢3  🟡1  🔴1              ││
│  └─────────────────────────────────┘│
│                                     │
│  📅 2024年12月                      │
│  ┌─────────────────────────────────┐│
│  │ [📷] February                   ││
│  │      2023 · 3+1AP               ││
│  │      🟢1  🟡1  🔴1              ││
│  └─────────────────────────────────┘│
├─────────────────────────────────────┤
│   🏠      📦      💬      ⚙️       │
└─────────────────────────────────────┘
```

**对话 Tab**（全屏对话）：

```
┌─────────────────────────────────────┐
│  对话                               │
├─────────────────────────────────────┤
│                                     │
│  👤 Guard 1/3 卖了                  │
│                                     │
│  🤖 好的，需要补充一些信息：         │
│     • 销售价格？                    │
│     • 买家？                        │
│     • 日期？                        │
│                                     │
│  👤 5万美金，柏林某基金会，上周五    │
│                                     │
│  🤖 收到，确认以下更新：             │
│     ┌───────────────────────────┐   │
│     │ Guard, I… - 1/3           │   │
│     │ 状态: 在库 → 已售          │   │
│     │ 价格: $50,000             │   │
│     │ 买家: 柏林某基金会         │   │
│     │ 日期: 2025-01-17          │   │
│     └───────────────────────────┘   │
│     [取消]  [确认更新]              │
│                                     │
├─────────────────────────────────────┤
│  [输入消息...]                 [↑]  │
├─────────────────────────────────────┤
│   🏠      📦      💬      ⚙️       │
└─────────────────────────────────────┘
```

**版本详情页**（从列表点击进入，覆盖底部导航）：

```
┌─────────────────────────────────────┐
│  ←  Guard, I… - 1/3                 │
├─────────────────────────────────────┤
│  ┌─────────────────────────────────┐│
│  │                                 ││
│  │         [作品图片]              ││
│  │                                 ││
│  └─────────────────────────────────┘│
│                                     │
│  状态      🟢 仓库存放              │
│  位置      柏林仓库 · A区-3号架     │
│  编号      AAJ-2024-015            │
│  品相      优秀                     │
│                                     │
│  ─────────────────────────────────  │
│  📎 附件 (3)                   [+]  │
│  ├── 📷 包装照片.jpg                │
│  ├── 📄 安装说明.pdf                │
│  └── 🔗 技术图纸 (Google Doc)       │
│                                     │
│  ─────────────────────────────────  │
│  📜 历史记录                        │
│  │                                  │
│  ├─ 2025-01-15  入库                │
│  │  位置: 柏林仓库                  │
│  │                                  │
│  └─ 2024-12-01  创建                │
│                                     │
├─────────────────────────────────────┤
│  [💬 对话操作]  [✏️ 编辑]           │  ← 底部操作栏
└─────────────────────────────────────┘
```

### 4.6 桌面端布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  📦 作品管理    [作品] [版本] [导入] [设置]          🔍 搜索...    ☀️ 👤    │
├───────────────────────────────────────────────────────┬─────────────────────┤
│                                                       │                     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                 │  💬 对话            │
│  │ 150  │ │ 45   │ │ 12   │ │ 3    │                 │                     │
│  │ 总数  │ │ 在库  │ │ 寄售  │ │ 已售  │                 │  👤 Guard 1/3 卖了  │
│  └──────┘ └──────┘ └──────┘ └──────┘                 │                     │
│                                                       │  🤖 需要补充：       │
│  [全部] [在库] [寄售] [已售]                           │     价格？买家？    │
│                                                       │                     │
│  ┌────────────────────────────────────────────────┐  │  👤 5万，某基金会    │
│  │ [📷] │ Guard, I…      │ 2024 │ 🟢2 🟡1 │ ... │  │                     │
│  ├──────┼────────────────┼──────┼─────────┼─────┤  │  🤖 确认更新：       │
│  │ [📷] │ Deep Web       │ 2024 │ 🟢3 🟡1 │ ... │  │  ┌─────────────────┐ │
│  ├──────┼────────────────┼──────┼─────────┼─────┤  │  │ 状态: → 已售    │ │
│  │ [📷] │ February       │ 2023 │ 🟢1 🔴1 │ ... │  │  │ 价格: $50,000  │ │
│  └────────────────────────────────────────────────┘  │  └─────────────────┘ │
│                                                       │  [确认]             │
│                                                       │                     │
│                                                       │  ───────────────    │
│                                                       │  [输入消息...]      │
└───────────────────────────────────────────────────────┴─────────────────────┘
```

### 4.7 移动端交互

**滑动操作**（参考 vocab-tracker）：

```
正常状态：
┌─────────────────────────────────────┐
│ [📷] Guard, I… - 1/3               │
│      🟢 仓库存放 · 柏林             │
└─────────────────────────────────────┘

← 左滑（快捷操作）：
┌─────────────────────────────────────┬──────────┐
│ [📷] Guard, I… - 1/3               │ 📝 编辑  │
│      🟢 仓库存放 · 柏林             │ 📎 附件  │
└─────────────────────────────────────┴──────────┘

→ 右滑（危险操作）：
┌──────────┬─────────────────────────────────────┐
│ 🗑️ 删除  │ [📷] Guard, I… - 1/3               │
│          │      🟢 仓库存放 · 柏林             │
└──────────┴─────────────────────────────────────┘
```

**长按菜单**：

```
┌─────────────────────────────────────┐
│        操作菜单                     │
├─────────────────────────────────────┤
│  📝  编辑版本                       │
│  📎  添加附件                       │
│  🔄  变更状态                       │
│  📤  导出资料                       │
│  ──────────────────────────────────│
│  🗑️  删除版本                       │
├─────────────────────────────────────┤
│            [取消]                   │
└─────────────────────────────────────┘
```

### 4.8 组件样式规范

**卡片**：
```css
.card {
  @apply bg-white dark:bg-slate-800 
         border border-slate-200 dark:border-slate-700 
         rounded-xl p-4;
}
```

**按钮**：
```css
.btn-primary {
  @apply bg-orange-500 hover:bg-orange-600 
         text-white font-medium 
         px-4 py-2 rounded-lg
         transition-colors;
}

.btn-secondary {
  @apply bg-slate-100 dark:bg-slate-700 
         hover:bg-slate-200 dark:hover:bg-slate-600
         text-slate-700 dark:text-slate-200
         px-4 py-2 rounded-lg
         transition-colors;
}
```

**输入框**：
```css
.input {
  @apply w-full px-4 py-2 
         bg-white dark:bg-slate-800 
         border border-slate-200 dark:border-slate-700 
         rounded-xl
         focus:ring-2 focus:ring-orange-500 focus:border-transparent
         outline-none;
}
```

**标签 (Pills)**：
```css
.pill {
  @apply px-3 py-1.5 rounded-full text-sm font-medium
         transition-colors cursor-pointer;
}

.pill-active {
  @apply bg-slate-800 dark:bg-slate-200 
         text-white dark:text-slate-800;
}

.pill-inactive {
  @apply bg-slate-100 dark:bg-slate-700/50 
         text-slate-600 dark:text-slate-400
         hover:bg-slate-200 dark:hover:bg-slate-700;
}
```

### 4.9 PWA 支持

支持离线访问和添加到主屏幕。

**功能清单**：

| 功能 | 说明 |
|------|------|
| 添加到主屏幕 | iOS / Android 均支持 |
| 离线访问 | 缓存静态资源和数据 |
| 离线操作 | 支持离线添加/编辑，恢复网络后同步 |
| 网络状态指示 | 显示在线/离线状态 |
| 后台同步 | 网络恢复后自动同步 |

**Web App Manifest**：

```json
// public/manifest.json
{
  "name": "aaajiao 作品管理",
  "short_name": "作品管理",
  "description": "艺术作品库存管理系统",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0f172a",
  "theme_color": "#0f172a",
  "orientation": "portrait-primary",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-192-maskable.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "maskable"
    }
  ]
}
```

**Apple Touch Icons**（深色/亮色模式适配）：

```html
<!-- index.html -->
<link rel="apple-touch-icon" href="/icons/apple-touch-icon.png">
<link rel="apple-touch-icon" href="/icons/apple-touch-icon-dark.png" media="(prefers-color-scheme: dark)">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="作品管理">
```

**Service Worker**：

```typescript
// public/sw.js
const CACHE_NAME = 'aaajiao-inventory-v1';
const STATIC_ASSETS = [
  '/',
  '/index.html',
  '/manifest.json',
  // Vite 构建后的资源会自动添加
];

// 安装时缓存静态资源
self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      return cache.addAll(STATIC_ASSETS);
    })
  );
});

// 网络优先，失败时使用缓存
self.addEventListener('fetch', (event) => {
  event.respondWith(
    fetch(event.request)
      .then((response) => {
        // 缓存成功的响应
        if (response.status === 200) {
          const clone = response.clone();
          caches.open(CACHE_NAME).then((cache) => {
            cache.put(event.request, clone);
          });
        }
        return response;
      })
      .catch(() => {
        // 网络失败，使用缓存
        return caches.match(event.request);
      })
  );
});
```

**离线数据缓存（IndexedDB）**：

```typescript
// services/offlineCache.ts
import { openDB, DBSchema, IDBPDatabase } from 'idb';

interface InventoryDB extends DBSchema {
  artworks: {
    key: string;
    value: Artwork;
    indexes: { 'by-updated': string };
  };
  editions: {
    key: string;
    value: Edition;
    indexes: { 'by-artwork': string };
  };
  syncQueue: {
    key: number;
    value: {
      id: number;
      action: 'create' | 'update' | 'delete';
      table: string;
      data: any;
      timestamp: number;
    };
  };
}

let db: IDBPDatabase<InventoryDB>;

export async function initDB() {
  db = await openDB<InventoryDB>('aaajiao-inventory', 1, {
    upgrade(db) {
      // 作品缓存
      const artworkStore = db.createObjectStore('artworks', { keyPath: 'id' });
      artworkStore.createIndex('by-updated', 'updated_at');
      
      // 版本缓存
      const editionStore = db.createObjectStore('editions', { keyPath: 'id' });
      editionStore.createIndex('by-artwork', 'artwork_id');
      
      // 离线操作队列
      db.createObjectStore('syncQueue', { keyPath: 'id', autoIncrement: true });
    },
  });
}

// 缓存数据
export async function cacheArtworks(artworks: Artwork[]) {
  const tx = db.transaction('artworks', 'readwrite');
  await Promise.all(artworks.map(a => tx.store.put(a)));
  await tx.done;
}

// 读取缓存
export async function getCachedArtworks(): Promise<Artwork[]> {
  return db.getAll('artworks');
}

// 添加到同步队列
export async function addToSyncQueue(action: string, table: string, data: any) {
  await db.add('syncQueue', {
    action,
    table,
    data,
    timestamp: Date.now(),
  });
}

// 处理同步队列
export async function processSyncQueue() {
  const items = await db.getAll('syncQueue');
  for (const item of items) {
    try {
      // 发送到服务器
      await syncToServer(item);
      // 成功后删除
      await db.delete('syncQueue', item.id);
    } catch (e) {
      console.error('Sync failed:', e);
      break; // 失败则停止，等待下次同步
    }
  }
}
```

**网络状态 Hook**：

```typescript
// hooks/useNetworkStatus.ts
import { useState, useEffect } from 'react';

export function useNetworkStatus() {
  const [isOnline, setIsOnline] = useState(navigator.onLine);
  const [pendingCount, setPendingCount] = useState(0);

  useEffect(() => {
    const handleOnline = () => {
      setIsOnline(true);
      // 网络恢复，触发同步
      processSyncQueue();
    };
    
    const handleOffline = () => setIsOnline(false);

    window.addEventListener('online', handleOnline);
    window.addEventListener('offline', handleOffline);

    return () => {
      window.removeEventListener('online', handleOnline);
      window.removeEventListener('offline', handleOffline);
    };
  }, []);

  return { isOnline, pendingCount };
}
```

**网络状态指示器**：

```tsx
// components/NetworkIndicator.tsx
export function NetworkIndicator() {
  const { isOnline, pendingCount } = useNetworkStatus();

  if (isOnline && pendingCount === 0) return null;

  return (
    <div className="fixed top-0 left-0 right-0 z-50">
      {!isOnline && (
        <div className="bg-yellow-500 text-yellow-900 text-center text-sm py-1">
          📴 离线模式 - 操作将在恢复网络后同步
        </div>
      )}
      {isOnline && pendingCount > 0 && (
        <div className="bg-blue-500 text-white text-center text-sm py-1">
          🔄 正在同步 ({pendingCount} 项待处理)
        </div>
      )}
    </div>
  );
}
```

**离线操作流程**：

```
用户操作（如更新状态）
        │
        ▼
    ┌───────────┐
    │ 网络可用？ │
    └───────────┘
     │         │
    Yes       No
     │         │
     ▼         ▼
  直接提交   保存到 IndexedDB
  到服务器   同步队列
     │         │
     ▼         ▼
  更新 UI    更新 UI
             (乐观更新)
                │
                ▼
          网络恢复时
          自动同步
```

**Vite PWA 插件配置**：

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'icons/*.png'],
      manifest: {
        name: 'aaajiao 作品管理',
        short_name: '作品管理',
        theme_color: '#0f172a',
        background_color: '#0f172a',
        display: 'standalone',
        icons: [
          { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png' },
        ],
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/.*\.supabase\.co\/.*/i,
            handler: 'NetworkFirst',
            options: {
              cacheName: 'supabase-api-cache',
              expiration: { maxEntries: 100, maxAgeSeconds: 60 * 60 * 24 },
            },
          },
        ],
      },
    }),
  ],
});
```

---

## 5. 用户认证与权限

### 5.1 用户角色

| 角色 | 人员 | 权限 |
|------|------|------|
| Admin | 艺术家本人 | 全部权限 + 用户管理 |
| Editor | 工作室员工 | 操作权限（录入、更新、查询） |
| Viewer | 画廊 | 只读（仅看自己相关的寄售作品） |

### 5.2 双入口设计

```
┌─────────────────────────────────────────────────────────────┐
│  内部管理系统 (需登录)                                       │
│  inventory.aaajiao.studio                                   │
│                                                             │
│  用户: 艺术家 + 工作室员工 (Admin / Editor)                 │
│  登录: Google OAuth / Magic Link                            │
│  权限: 完整的读写操作                                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  画廊门户 (分享链接，无需登录)                               │
│  inventory.aaajiao.studio/gallery/abc123                    │
│                                                             │
│  用户: 画廊 (Viewer)                                        │
│  访问: 专属链接（由系统生成）                                │
│  权限: 只读，仅显示寄售在该画廊的作品                        │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 内部系统认证

**MVP 阶段：邮箱白名单**

```bash
# .env.local
ALLOWED_EMAILS=aaajiao@xxx.com,assistant1@xxx.com,assistant2@xxx.com
```

```typescript
// 登录逻辑
const ALLOWED_EMAILS = process.env.ALLOWED_EMAILS?.split(',') || [];

if (!ALLOWED_EMAILS.includes(userEmail)) {
  return new Response('未授权访问，请联系管理员', { status: 403 });
}
```

**登录方式**：Supabase Auth

```
┌─────────────────────────────────────────────────────────────┐
│  aaajiao 作品管理系统                                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│              欢迎回来                                        │
│                                                             │
│         [使用 Google 账号登录]                              │
│                                                             │
│              ─ 或 ─                                         │
│                                                             │
│         邮箱: [____________]                                │
│               [发送登录链接]                                │
│                                                             │
│         仅限受邀用户                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**后续升级（需要时再做）**：
- 系统内邀请用户功能
- 角色区分（Admin / Editor）
- 用户管理界面

### 5.4 画廊门户

**链接格式**：
```
https://inventory.aaajiao.studio/gallery/{token}

token: 随机生成的唯一标识，如 tr-2024-a1b2c3d4
```

**生成方式**：
- 自动：首次将作品标记为寄售给某画廊时，自动生成该画廊的专属链接
- 手动：在画廊管理页面手动创建/重置链接

**画廊看到的内容**：

```
┌─────────────────────────────────────────────────────────────┐
│  aaajiao - Tabula Rasa 寄售作品                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  当前寄售 (3 件)                                            │
│                                                             │
│  ┌──────┐  Guard, I… - 2/3                                 │
│  │ 📷   │  2024 · Installation · 75×75×140cm               │
│  └──────┘  寄售开始: 2024-08-15                            │
│                                                             │
│  ┌──────┐  Deep Web - 1/5                                  │
│  │ 📷   │  2024 · Installation · 尺寸可变                   │
│  └──────┘  寄售开始: 2024-09-01                            │
│                                                             │
│  ┌──────┐  February - AP 1/2                               │
│  │ 📷   │  2023 · Video · 12'30''                          │
│  └──────┘  寄售开始: 2024-10-20                            │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│  最后更新: 2025-01-23                                       │
│  如有疑问请联系工作室: studio@aaajiao.com                   │
└─────────────────────────────────────────────────────────────┘
```

**链接管理功能**：

| 功能 | 说明 |
|------|------|
| 生成链接 | 创建画廊专属链接 |
| 重置链接 | 作废旧链接，生成新链接 |
| 禁用链接 | 临时禁止访问 |
| 访问记录 | 可选，记录画廊查看时间 |

**安全考虑**：
- 链接 token 足够长（32 字符），不可猜测
- 可随时作废
- 只显示该画廊相关的作品
- 不显示价格等敏感信息（可配置）

---

## 6. 数据模型

### 6.1 ER 图

```
┌─────────────────────────────────────────────────────────────┐
│                      users (用户)                            │
│─────────────────────────────────────────────────────────────│
│ id, email, role (admin/editor), status, last_login          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│               locations (位置 - 动态学习)                    │
│─────────────────────────────────────────────────────────────│
│ id, name, type (studio/gallery/museum/other)               │
│ aliases[] ◄──── AI 学习的别名（如 ["TR", "Tabula"]）        │
│ city, country, address, contact                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  gallery_links (画廊链接)                    │
│─────────────────────────────────────────────────────────────│
│ id, gallery_name, token, status, last_accessed              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    artworks (作品 - 精简)                    │
│─────────────────────────────────────────────────────────────│
│ id                                                          │
│ source_url ◄──── 核心：跳转网站查看完整信息/原图              │
│ title_en / title_cn                                         │
│ year, type, materials, dimensions, duration                 │
│ thumbnail_url ◄──── 压缩后的缩略图（仅一张）                  │
│ edition_total / ap_total / is_unique                        │
└───────────────────────────────────────────────────────────┬─┘
                                                            │
                                │ 1:N
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    editions (版本 - 核心)                    │
│─────────────────────────────────────────────────────────────│
│ id                                                          │
│ artwork_id                                                  │
│ inventory_number ◄──── 唯一编号（可选，智能提示）            │
│ edition_type + edition_number → "1/3", "AP 1/2"            │
│ status (in_studio/at_gallery/at_museum/sold/...)           │
│ location_id ◄──── FK → locations（动态学习）                │
│ storage_detail ◄──── 仓储细节（A区-3号架-2层）              │
│ condition + condition_notes                                 │
│ sale_price / sale_currency / sale_date / buyer_name        │
│ consignment_start / loan_institution / loan_end            │
│ certificate_number                                          │
└───────────────────────────────────────────────────────────┬─┘
                                                            │
              ┌─────────────────┼─────────────────┐
              │ 1:N             │ 1:N             │
              ▼                 ▼                 
┌─────────────────────────┐  ┌─────────────────────────┐
│     edition_files       │  │     edition_history     │
│─────────────────────────│  │─────────────────────────│
│ source_type:            │  │ action                  │
│  • upload 上传文件      │  │ from_status → to_status │
│  • link 外部链接        │  │ from_location → to_loc  │
│ file_url                │  │ related_party           │
│ file_type               │  │ price / currency        │
│ description ◄── 用户自填 │  │ notes                   │
│ "安装说明书(Google Doc)" │  │ created_at / created_by │
└─────────────────────────┘  └─────────────────────────┘
```

### 6.2 表结构详情

#### `users` 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uuid | 主键（Supabase Auth user id） |
| `email` | text | 邮箱（唯一） |
| `name` | text | 显示名称 |
| `role` | enum | 角色: `admin` / `editor` |
| `status` | enum | 状态: `active` / `inactive` |
| `last_login` | timestamp | 最后登录时间 |
| `created_at` | timestamp | 创建时间 |

#### `locations` 位置表（动态学习）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uuid | 主键 |
| `name` | text | 位置名称（如 "柏林工作室"、"Tabula Rasa"） |
| `type` | enum | 类型: `studio` / `gallery` / `museum` / `other` |
| `aliases` | text[] | 别名（如 ["TR", "Tabula"]） |
| `city` | text | 城市（可选） |
| `country` | text | 国家（可选） |
| `address` | text | 地址（可选） |
| `contact` | text | 联系方式（可选） |
| `notes` | text | 备注 |
| `created_at` | timestamp | 创建时间 |

**设计决策**：
- **不预设列表**：使用时动态添加
- **AI 智能匹配**：支持模糊匹配和别名
- **自动分类**：根据输入上下文判断类型
- 工作室和仓库统一为 `studio` 类型，用名称区分

**AI 位置匹配流程**：

```
用户输入: "送到 TR"

AI 处理:
1. 搜索 locations 表
   - name ILIKE '%TR%' 
   - OR 'TR' = ANY(aliases)
2. 找到 "Tabula Rasa" → 使用
3. 未找到 → 询问 "是否添加新位置「TR」？类型是？"
4. 用户确认 → INSERT INTO locations
```

#### `gallery_links` 画廊链接表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uuid | 主键 |
| `gallery_name` | text | 画廊名称 |
| `token` | text | 链接 token（唯一，32字符） |
| `status` | enum | 状态: `active` / `disabled` |
| `show_prices` | boolean | 是否显示价格（默认 false） |
| `last_accessed` | timestamp | 最后访问时间 |
| `access_count` | integer | 访问次数 |
| `created_at` | timestamp | 创建时间 |
| `created_by` | uuid | 创建人 (FK → users) |

#### `artworks` 作品表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uuid | 主键 |
| `source_url` | text | **网站链接（核心字段，用于匹配和跳转）** |
| `title_en` | text | 英文标题 |
| `title_cn` | text | 中文标题 |
| `year` | text | 年份（如 "2024-2025"） |
| `type` | text | 作品类型 |
| `materials` | text | 材料 |
| `dimensions` | text | 尺寸 |
| `duration` | text | 时长（视频类） |
| `thumbnail_url` | text | 缩略图 URL（压缩后存 Storage） |
| `edition_total` | integer | 总版数 |
| `ap_total` | integer | AP 总数 |
| `is_unique` | boolean | 是否唯一版 |
| `notes` | text | 备注 |
| `created_at` | timestamp | 创建时间 |
| `updated_at` | timestamp | 更新时间 |

**设计决策**：
- 不存储 `description` — 通过 `source_url` 查看
- 不存储 `original_images` — 只保存选中的缩略图

#### `editions` 版本表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uuid | 主键 |
| `artwork_id` | uuid | 关联作品 (FK) |
| `inventory_number` | text | **唯一编号（可选，手动，全局唯一）** |
| `edition_type` | enum | 版本类型: `numbered` / `ap` / `unique` |
| `edition_number` | integer | 版号（如 1, 2, 3） |
| `status` | enum | 当前状态 |
| `location_id` | uuid | **关联位置 (FK → locations)** |
| `storage_detail` | text | 仓储细节（如 "A区-3号架-2层"） |
| `condition` | enum | 品相: `excellent`/`good`/`fair`/`poor`/`damaged` |
| `condition_notes` | text | 品相备注 |
| `sale_price` | decimal | 成交价 |
| `sale_currency` | enum | **货币（可选列表）** |
| `sale_date` | date | 成交日期 |
| `buyer_name` | text | 买家 |
| `consignment_start` | date | 寄售开始日期 |
| `loan_institution` | text | 借展机构（美术馆借展时） |
| `loan_end` | date | 借展结束日期 |
| `certificate_number` | text | 证书编号 |
| `notes` | text | 备注 |
| `created_at` | timestamp | 创建时间 |
| `updated_at` | timestamp | 更新时间 |

**状态枚举 `status`**:
- `in_production` 🔨 制作中
- `in_studio` 🏠 工作室（艺术家的工作室/仓库）
- `at_gallery` 🖼️ 画廊（展出或寄售）
- `at_museum` 🏛️ 美术馆（展览或借展）
- `in_transit` 🚚 在途
- `sold` ✅ 已售
- `gifted` 🎁 赠送
- `lost` ❌ 遗失
- `damaged` ⚠️ 损坏

**货币枚举 `sale_currency`**:
- `USD` 美元
- `EUR` 欧元
- `CNY` 人民币
- `GBP` 英镑
- `CHF` 瑞士法郎
- `HKD` 港币
- `JPY` 日元

**位置管理**：动态学习，不预设列表（见 `locations` 表）

#### `edition_files` 版本附件表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uuid | 主键 |
| `edition_id` | uuid | 关联版本 (FK) |
| `source_type` | enum | `upload` 上传 / `link` 外部链接 |
| `file_url` | text | 文件 URL（Storage 或外部链接） |
| `file_type` | enum | 文件格式 |
| `file_name` | text | 文件名 |
| `file_size` | integer | 文件大小 bytes（仅上传） |
| `description` | text | **用户自己写的说明** |
| `sort_order` | integer | 排序 |
| `created_at` | timestamp | 创建时间 |
| `created_by` | text | 创建人 |

**文件格式枚举 `file_type`**:
- `image` 图片
- `pdf` PDF
- `video` 视频
- `document` 文档（Google Doc、Notion 等）
- `spreadsheet` 表格
- `link` 通用链接
- `other` 其他

**设计决策**：
- 不用预设类型枚举（如 "packaging"、"installation"）
- 用户在 `description` 中自由描述用途
- 支持上传文件和外部链接两种来源

#### `edition_history` 版本历史表

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | uuid | 主键 |
| `edition_id` | uuid | 关联版本 (FK) |
| `action` | enum | 操作类型 |
| `from_status` | text | 原状态 |
| `to_status` | text | 新状态 |
| `from_location` | text | 原位置 |
| `to_location` | text | 新位置 |
| `related_party` | text | 相关方（画廊/买家/展览） |
| `price` | decimal | 金额 |
| `currency` | text | 货币 |
| `notes` | text | 备注 |
| `created_at` | timestamp | 创建时间 |
| `created_by` | text | 操作人 |

**操作类型枚举 `action`**:
- `created` 创建
- `status_change` 状态变更
- `location_change` 位置变更
- `sold` 售出
- `consigned` 寄售
- `returned` 归还
- `condition_update` 品相更新
- `file_added` 添加附件
- `number_assigned` 分配编号

### 6.3 文件存储

**Supabase Storage 结构**:

```
├── thumbnails/                    # 作品缩略图
│   ├── {artwork_id}.webp         # 400x400, < 50KB
│   └── ...
│
└── edition-files/                 # 版本附件（仅上传文件）
    ├── {edition_id}/
    │   ├── {timestamp}_{name}.webp   # 图片（压缩后）
    │   ├── {timestamp}_{name}.pdf    # PDF（原样）
    │   └── ...
    └── ...
```

**处理规则**:

| 文件类型 | 处理 | 大小限制 |
|----------|------|----------|
| 图片 | 压缩至 1600px, WebP | < 20MB 原始 |
| PDF | 原样保存 | < 20MB |
| 视频 | 原样保存 | < 50MB |
| 缩略图 | 400x400, WebP | < 50KB |

**外部链接**:
- 仅存 URL，不下载
- 支持 Google Doc/Sheet、Notion、Dropbox 等

---

## 7. 核心功能

### 7.1 数据导入（增量更新）

**两种导入方式**：

| 方式 | 说明 |
|------|------|
| MD 文件 | 批量导入，解析网站导出的 Markdown |
| URL 输入 | 单条导入，Firecrawl 抓取网站页面 |

**MD 文件格式**（来自 eventstructure.com 导出）：

```markdown
## Guard, I…

| 字段 | 内容 |
|------|------|
| 年份 | 2024-2025 |
| 类型 | Installation |
| 尺寸 | 75 x 75 x 140 cm |
| 材料 | silicone, 3D printing, fiberglass |
| 时长 | 12'00'' |
| 视频 | [https://vimeo.com/...](url) |
| 链接 | [https://eventstructure.com/Guard-I](url) |

### 中文描述
> ...

### English Description
...

### 图片
![](https://payload.cargocollective.com/...)
![](https://payload.cargocollective.com/...)
```

**MD 解析映射**：

| MD 字段 | 数据库字段 | 处理逻辑 |
|---------|------------|----------|
| `## 标题` | `title_en` | 提取 `##` 后的文本 |
| 年份 | `year` | 解析为字符串，支持 "2024-2025" |
| 类型 | `type` | 原样存储 |
| 尺寸 | `dimensions` | 原样存储 |
| 材料 | `materials` | 原样存储（可能为空） |
| 时长 | `duration` | 仅视频作品有 |
| 视频 | `video_url` | 提取 URL |
| 链接 | `source_url` | **用于匹配增量更新** |
| `### 图片` 下的 `![]()` | `images[]` | 提取所有图片 URL，第一张作为缩略图候选 |

**MD 解析代码示例**：

```typescript
// lib/md-parser.ts
interface ParsedArtwork {
  title_en: string;
  year: string;
  type: string;
  dimensions?: string;
  materials?: string;
  duration?: string;
  video_url?: string;
  source_url: string;
  images: string[];
}

export function parsePortfolioMD(content: string): ParsedArtwork[] {
  const artworks: ParsedArtwork[] = [];
  
  // 按 "## " 分割作品
  const sections = content.split(/\n## /).slice(1);
  
  for (const section of sections) {
    const lines = section.split('\n');
    const title = lines[0].trim();
    
    // 解析表格
    const tableMatch = section.match(/\| 字段 \| 内容 \|[\s\S]*?(?=\n\n|###)/);
    const fields: Record<string, string> = {};
    
    if (tableMatch) {
      const rows = tableMatch[0].split('\n').slice(2); // 跳过表头
      for (const row of rows) {
        const match = row.match(/\| (.+?) \| (.+?) \|/);
        if (match) {
          fields[match[1].trim()] = match[2].trim();
        }
      }
    }
    
    // 提取图片
    const images = [...section.matchAll(/!\[.*?\]\((https?:\/\/[^\)]+)\)/g)]
      .map(m => m[1]);
    
    // 提取链接 URL
    const linkMatch = fields['链接']?.match(/\[(.+?)\]\((.+?)\)/);
    const sourceUrl = linkMatch?.[2] || '';
    
    artworks.push({
      title_en: title,
      year: fields['年份'] || '',
      type: fields['类型'] || '',
      dimensions: fields['尺寸'],
      materials: fields['材料'],
      duration: fields['时长'],
      video_url: extractUrl(fields['视频']),
      source_url: sourceUrl,
      images,
    });
  }
  
  return artworks;
}
```

**增量更新逻辑**（MD 和 URL 统一）：

```
导入数据 → 通过 source_url 匹配数据库
    │
    ├── 不存在 → 新增（需选择缩略图）
    │
    └── 已存在 → 对比字段
                    │
                    ├── 有变化 → 显示变更，确认后更新
                    │
                    └── 无变化 → 跳过
```

**更新规则**：

| 字段类型 | 策略 |
|----------|------|
| 网站字段（title, year, type, materials, dimensions, duration） | 导入覆盖数据库 |
| 管理字段（edition_total, ap_total, notes） | 保留数据库值 |
| 缩略图 | 保留现有 |
| 版本数据 | 完全保留 |

### 7.2 版本管理

**版本列表页**：

```
┌─────────────────────────────────────────────────────────────┐
│  版本总览                        [筛选: 状态 ▼] [位置 ▼]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  │ 缩略图 │ 作品         │ 版本 │ 编号         │ 状态    │  │
│  │ [📷]  │ Guard, I…    │ 1/3  │ AAJ-2024-015 │ 🔴 已售 │  │
│  │ [📷]  │ Guard, I…    │ 2/3  │ AAJ-2024-016 │ 🟡 寄售 │  │
│  │ [📷]  │ Guard, I…    │ 3/3  │ (暂无)       │ 🟢 在库 │  │
│  │ [📷]  │ Deep Web     │ 1/5  │ AAJ-2024-010 │ 🔵 展览 │  │
│  │ ...                                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**版本详情页**：

```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回                                      [对话操作]     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Guard, I… - 3/3 (numbered)                                │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 唯一编号: AAJ-2024-017                              │   │
│  │ 状态: 🟢 仓库存放                                   │   │
│  │ 位置: 柏林仓库                                       │   │
│  │ 仓储位置: A区-3号架-2层                              │   │
│  │ 品相: 优秀                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  附件资料                                    [+ 添加]       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📷 包装箱正面          IMG_001.jpg   [下载] [删除]  │   │
│  │ 📷 装箱细节            IMG_002.jpg   [下载] [删除]  │   │
│  │ 📄 安装说明书          docs.google.. [打开] [删除]  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  状态历史                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ● 2024-11-15  入库 → 仓库存放                       │   │
│  │   │           位置: 柏林仓库 A区-3号架-2层           │   │
│  │ ● 2024-10-20  制作中 → 入库                         │   │
│  │ ● 2024-09-01  创建版本                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 唯一编号（智能提示）

**设计决策**：编号在版本层级（每个物理实体独立编号）

**编号规则**：
- 可选：没有销售/展览记录的版本可以不编号
- 手动：完全自定义格式
- 唯一：系统校验全局不重复

**智能提示**：

```
已有编号: AAJ-2024-001, AAJ-2024-002, AAJ-2024-003

填写新编号时：
┌────────────────────────────────────┐
│ 💡 识别格式: AAJ-YYYY-NNN          │
│    建议下一个: AAJ-2024-004        │
│                      [使用建议]    │
└────────────────────────────────────┘
```

### 7.4 附件管理

**添加附件**（两种模式）：

```
┌─────────────────────────────────────────────────────────────┐
│  添加附件                                                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [📁 上传文件]    [🔗 添加链接]                             │
│                                                             │
│  【上传文件】                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           拖拽文件到此处，或点击选择                  │   │
│  │           图片 < 20MB · PDF < 20MB · 视频 < 50MB     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  【添加链接】                                               │
│  链接: [_______________________________________]           │
│  类型: [文档 ▼]                                            │
│                                                             │
│  说明: [_______________________________________]           │
│        例如: 包装箱正面 / 安装说明书 / 技术图纸             │
│                                                             │
│                                        [取消] [添加]       │
└─────────────────────────────────────────────────────────────┘
```

### 7.5 数据导出

**导出范围**：
- 单个作品
- 多选作品
- 筛选结果
- 全部作品

**导出格式**：

| 格式 | 图片 | 用途 |
|------|------|------|
| **PDF** | 嵌入图片 | 打印、发送给画廊/藏家 |
| **MD** | 图片链接 | 编辑、发布、转换其他格式 |
| **Excel (.xlsx)** | 无 | 数据分析、批量处理 |
| **CSV** | 无 | 通用数据交换 |
| **JSON** | 包含 URL | 完整备份、数据迁移、程序对接 |

**PDF 示例**（作品资料单页）：

```
┌─────────────────────────────────────────┐
│  [嵌入图片]                             │
│                                         │
│  Guard, I…                              │
│  守卫，我...                            │
│                                         │
│  2024                                   │
│  Installation                           │
│  木材、金属、电子元件                    │
│  75 × 75 × 140 cm                       │
│                                         │
│  版本: 3 + 2AP                          │
│  ─────────────────────────────────────  │
│  eventstructure.com/work/guard-i        │
│                                         │
│  © aaajiao studio                       │
└─────────────────────────────────────────┘
```

**MD 示例**（带图片链接）：

```markdown
# Guard, I…
守卫，我...

![Guard, I…](https://xxx.supabase.co/storage/thumbnails/abc123.webp)

**年份**: 2024  
**类型**: Installation  
**材料**: 木材、金属、电子元件  
**尺寸**: 75 × 75 × 140 cm  
**版本**: 3 + 2AP

[查看详情](https://eventstructure.com/work/guard-i)

---
```

**Excel/CSV 示例**：

| 标题 | 中文标题 | 年份 | 类型 | 材料 | 尺寸 | 版本 | AP | 在库 | 寄售 | 已售 |
|------|----------|------|------|------|------|------|-----|------|------|------|
| Guard, I… | 守卫，我... | 2024 | Installation | 木材、金属... | 75×75×140cm | 3 | 2 | 2 | 1 | 0 |

**JSON 备份示例**：

```json
{
  "exported_at": "2025-01-23T10:00:00Z",
  "version": "1.0",
  "artworks": [
    {
      "id": "uuid",
      "source_url": "https://eventstructure.com/work/guard-i",
      "title_en": "Guard, I…",
      "title_cn": "守卫，我...",
      "year": "2024",
      "type": "Installation",
      "materials": "木材、金属、电子元件",
      "dimensions": "75 × 75 × 140 cm",
      "thumbnail_url": "https://xxx.supabase.co/storage/thumbnails/abc123.webp",
      "edition_total": 3,
      "ap_total": 2,
      "editions": [
        {
          "id": "uuid",
          "edition_type": "numbered",
          "edition_number": 1,
          "inventory_number": "AAJ-2024-015",
          "status": "in_storage",
          "location_name": "柏林仓库",
          "storage_location": "A区-3号架-2层",
          "condition": "excellent",
          "files": [...],
          "history": [...]
        }
      ]
    }
  ],
  "gallery_links": [...],
  "users": [...]
}
```

**通过对话导出**：

```
用户: 导出 Guard 的 PDF

Agent: ✅ 已生成 [下载 Guard-I-作品资料.pdf]

用户: 把 Tabula Rasa 的寄售作品导出 Excel

Agent: ✅ 已生成，共 3 件 [下载 Tabula-Rasa-寄售清单.xlsx]

用户: 备份所有数据

Agent: ✅ 已生成完整备份 (150 件作品)
       [下载 aaajiao-inventory-2025-01-23.json]
```

**导出界面**：

```
┌─────────────────────────────────────────────────────────────┐
│  导出                                                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  选择范围:                                                  │
│  ○ 当前作品 (Guard, I…)                                    │
│  ○ 选中的作品 (5 件)                                       │
│  ○ 筛选结果 (12 件)                                        │
│  ○ 全部作品 (150 件)                                       │
│                                                             │
│  选择格式:                                                  │
│  ☐ PDF (带图，可打印)                                      │
│  ☐ Markdown (带图链接)                                     │
│  ☐ Excel                                                   │
│  ☐ CSV                                                     │
│  ☐ JSON (完整备份)                                         │
│                                                             │
│                                        [取消] [导出]        │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 费用估算

### 8.1 月度费用

| 服务 | 免费额度 | 预估用量 | 费用 |
|------|----------|----------|------|
| **Vercel** | Hobby 免费 | 前端 + Functions | **$0** |
| **Supabase** | 500MB DB + 1GB Storage | ~100MB + ~300MB | **$0** |
| **AI API** | 视模型 | ~100-200 次对话/月 | **$1-15** |
| **Firecrawl** | 500 credits/月 | ~50 次抓取 | **$0** |

**总计：$1-15/月**（取决于模型选择）

### 8.2 AI 模型费用对比

**定价**（per 1M tokens）：

| 模型 | 输入 | 输出 | 单次对话成本* |
|------|------|------|--------------|
| Claude Sonnet 4 | $3 | $15 | ~$0.01-0.02 |
| GPT-4o | $2.5 | $10 | ~$0.008-0.015 |
| Gemini 2.0 Flash | $0.075 | $0.30 | ~$0.0003 |
| Groq (Llama 3.3 70B) | $0.59 | $0.79 | ~$0.002 |

*单次对话：~2000 输入 tokens + ~500 输出 tokens

**月度成本对比**（200 次对话/月）：

| 模型策略 | 月费用 | 说明 |
|----------|--------|------|
| 全用 Claude Sonnet | ~$4-5 | 最准确 |
| 全用 GPT-4o | ~$3-4 | 快速稳定 |
| 全用 Gemini Flash | ~$0.1 | **几乎免费** |
| **混合策略（推荐）** | ~$1-3 | 复杂用 Claude，简单用 Gemini |

**推荐的混合策略**：

```
复杂操作（状态变更、导入、批量）→ Claude Sonnet (30%)
简单查询（搜索、统计）         → Gemini Flash (60%)
报表生成                      → GPT-4o (10%)

200 次对话/月:
• 60 次 Claude: ~$1.2
• 120 次 Gemini: ~$0.04
• 20 次 GPT: ~$0.3
────────────────────────
总计: ~$1.5/月
```

### 8.3 免费/本地模型选项

如果想完全免费，可以使用：

| 方案 | 说明 | 限制 |
|------|------|------|
| Gemini Flash | Google 免费额度慷慨 | 有速率限制 |
| Groq 免费版 | 每天有免费额度 | 高峰期可能排队 |
| Ollama (本地) | 完全免费 | 需要本地 GPU，响应较慢 |

### 8.4 扩展成本

如果用量增长：

| 服务 | 下一档 | 价格 |
|------|--------|------|
| Vercel Pro | 更多资源 | $20/月 |
| Supabase Pro | 8GB DB + 100GB Storage | $25/月 |
| AI API | 按量计费 | 视用量 |

---

## 9. 开发计划

### 9.1 阶段划分（7 周）

#### Phase 1: 基础框架 + 数据模型（1.5 周）

| 任务 | 时间 |
|------|------|
| 项目初始化（Vite + React + Tailwind） | 2h |
| Supabase 配置（数据库 + Storage + Auth） | 4h |
| 数据库表结构创建 | 4h |
| 基础 CRUD API | 8h |
| **用户认证（邮箱白名单 + OAuth）** | 6h |
| 作品/版本列表页 UI | 8h |
| 作品/版本详情页 UI | 8h |

**交付物**: 可登录、可浏览的作品和版本列表

#### Phase 2: AI 对话核心（1.5 周）

| 任务 | 时间 |
|------|------|
| Vercel AI SDK 集成 | 4h |
| 多模型配置 | 2h |
| 工具定义（查询类） | 6h |
| 工具定义（写入类 + 确认机制） | 8h |
| 对话面板 UI（含底部导航） | 6h |
| **可编辑确认卡片（内联编辑 + 完整编辑）** | 6h |
| 状态变更流程完整测试 | 4h |

**交付物**: 可通过对话执行查询和状态变更，支持快速修改 AI 解析结果

#### Phase 3: 版本管理完善（1 周）

| 任务 | 时间 |
|------|------|
| 版本历史时间线 | 4h |
| 附件上传功能 | 6h |
| 附件外部链接功能 | 3h |
| 图片压缩处理 | 3h |
| 唯一编号 + 智能提示 | 4h |

**交付物**: 完整的版本管理功能

#### Phase 4: 数据导入导出（1 周）

| 任务 | 时间 |
|------|------|
| MD 文件解析器 | 6h |
| URL 抓取（Firecrawl） | 6h |
| 增量更新检测 + 对比显示 | 6h |
| 缩略图选择器 | 4h |
| **多格式导出（PDF/MD/Excel/CSV/JSON）** | 8h |
| 导入导出 UI | 4h |

**交付物**: MD/URL 导入 + 多格式导出

#### Phase 5: 画廊门户 + 设计优化（1 周）

| 任务 | 时间 |
|------|------|
| **画廊链接管理** | 4h |
| **画廊门户页面（只读）** | 6h |
| 主题切换（深色/亮色） | 2h |
| 骨架屏加载组件 | 3h |
| 响应式适配 | 6h |
| 移动端滑动操作 | 4h |
| UI/UX 优化 | 4h |

**交付物**: 画廊门户 + 完整的响应式设计

#### Phase 6: PWA + 部署（1 周）

| 任务 | 时间 |
|------|------|
| **PWA Manifest 配置** | 2h |
| **Service Worker 离线缓存** | 4h |
| **IndexedDB 数据缓存** | 6h |
| **离线操作 + 同步队列** | 6h |
| **网络状态指示器** | 2h |
| 应用图标（多尺寸 + 深色适配） | 2h |
| Vercel 部署配置 | 2h |
| 文档编写 | 4h |

**交付物**: 完整的 PWA 应用，支持离线访问和添加到主屏幕

---

## 10. 核心代码示例

### 10.1 模型配置

```typescript
// src/lib/models.ts
import { anthropic } from "@ai-sdk/anthropic";
import { openai } from "@ai-sdk/openai";
import { google } from "@ai-sdk/google";
import { groq } from "@ai-sdk/groq";

export type ModelType = "complex" | "simple" | "fast";

export const models = {
  // 复杂任务 - 准确优先（状态变更、导入、批量操作）
  complex: anthropic("claude-sonnet-4-20250514"),
  
  // 简单任务 - 成本优先（搜索、统计）
  simple: google("gemini-2.0-flash"),
  
  // 快速响应（实时补全、建议）
  fast: groq("llama-3.3-70b-versatile"),
  
  // 备选
  fallback: openai("gpt-4o"),
};

// 根据任务类型自动选择模型
export function selectModel(taskType: ModelType = "complex") {
  return models[taskType] || models.complex;
}

// 根据用户消息内容推断任务类型
export function inferTaskType(message: string): ModelType {
  const complexKeywords = ["更新", "变更", "导入", "删除", "批量", "售出", "寄售"];
  const simpleKeywords = ["查询", "搜索", "找", "有哪些", "列出", "统计"];
  
  if (complexKeywords.some(k => message.includes(k))) return "complex";
  if (simpleKeywords.some(k => message.includes(k))) return "simple";
  return "complex"; // 默认用准确的
}
```

### 10.2 API 端点（多模型版）

```typescript
// api/chat.ts
import { streamText } from "ai";
import { createClient } from "@supabase/supabase-js";
import { z } from "zod";
import { selectModel, inferTaskType } from "../src/lib/models";

export const config = { runtime: "edge" };

export default async function handler(req: Request) {
  const { messages, modelType } = await req.json();
  
  const supabase = createClient(
    process.env.SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_KEY!
  );

  // 获取最新用户消息
  const lastUserMessage = messages.filter(m => m.role === "user").pop();
  
  // 自动推断或使用指定的模型类型
  const taskType = modelType || inferTaskType(lastUserMessage?.content || "");
  const model = selectModel(taskType);

  const result = streamText({
    model,
    system: `你是 aaajiao 工作室的作品库存管理助手。
帮助用户管理作品版本、记录销售、追踪寄售状态等。

执行写入操作前，先展示操作预览，等待用户确认。
用简洁的中文回复。`,
    messages,
    tools: {
      search_editions: {
        description: "搜索版本",
        parameters: z.object({
          status: z.string().optional(),
          location: z.string().optional(),
          artworkTitle: z.string().optional(),
        }),
        execute: async ({ status, location, artworkTitle }) => {
          let query = supabase
            .from("editions")
            .select(`
              *,
              artwork:artworks(title_en, title_cn, thumbnail_url)
            `);
          
          if (status) query = query.eq("status", status);
          if (location) query = query.ilike("location_name", `%${location}%`);
          
          const { data } = await query;
          return data;
        },
      },
      
      update_edition: {
        description: "更新版本状态（需要用户确认）",
        parameters: z.object({
          editionId: z.string(),
          status: z.string().optional(),
          locationName: z.string().optional(),
          salePrice: z.number().optional(),
          saleCurrency: z.string().optional(),
          buyerName: z.string().optional(),
          saleDate: z.string().optional(),
        }),
        execute: async (params) => {
          // 返回预览，不直接执行
          return {
            type: "confirmation_required",
            action: "update_edition",
            params,
            message: "请确认以上更新",
          };
        },
      },
      
      confirm_update: {
        description: "确认并执行更新",
        parameters: z.object({
          action: z.string(),
          params: z.any(),
        }),
        execute: async ({ action, params }) => {
          if (action === "update_edition") {
            const { editionId, ...updates } = params;
            
            // 记录历史
            const { data: oldEdition } = await supabase
              .from("editions")
              .select("*")
              .eq("id", editionId)
              .single();
            
            // 更新
            await supabase
              .from("editions")
              .update(updates)
              .eq("id", editionId);
            
            // 插入历史记录
            await supabase.from("edition_history").insert({
              edition_id: editionId,
              action: "status_change",
              from_status: oldEdition.status,
              to_status: updates.status,
              // ...
            });
            
            return { success: true, message: "更新成功" };
          }
        },
      },
    },
  });

  return result.toDataStreamResponse();
}
```

### 10.3 前端对话组件

```tsx
// src/components/chat/ChatPanel.tsx
import { useChat } from "@ai-sdk/react";
import { useState } from "react";
import { Message } from "./Message";
import { ConfirmDialog } from "./ConfirmDialog";
import { ModelType } from "../../lib/models";

interface ChatPanelProps {
  defaultModel?: ModelType;
}

export function ChatPanel({ defaultModel = "complex" }: ChatPanelProps) {
  const [pendingConfirmation, setPendingConfirmation] = useState(null);
  const [modelType, setModelType] = useState<ModelType>(defaultModel);
  
  const { messages, input, setInput, append, isLoading } = useChat({
    api: "/api/chat",
    body: { modelType },  // 传递模型类型
    onToolCall: ({ toolCall }) => {
      if (toolCall.result?.type === "confirmation_required") {
        setPendingConfirmation(toolCall.result);
        return;
      }
    },
  });

  const handleConfirm = async () => {
    await append({
      role: "user",
      content: "确认",
    });
    setPendingConfirmation(null);
  };

  return (
    <div className="flex flex-col h-full">
      {/* 模型选择器（可选显示） */}
      <div className="p-2 border-b flex items-center gap-2 text-sm text-gray-500">
        <span>模型:</span>
        <select 
          value={modelType} 
          onChange={(e) => setModelType(e.target.value as ModelType)}
          className="text-xs border rounded px-1"
        >
          <option value="complex">Claude (准确)</option>
          <option value="simple">Gemini (快速)</option>
          <option value="fast">Groq (极速)</option>
        </select>
      </div>
      
      <div className="flex-1 overflow-y-auto p-4 space-y-4">
        {messages.map((m) => (
          <Message key={m.id} message={m} />
        ))}
      </div>
      
      <form
        onSubmit={(e) => {
          e.preventDefault();
          append({ role: "user", content: input });
          setInput("");
        }}
        className="p-4 border-t"
      >
        <input
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="输入消息..."
          className="w-full p-2 border rounded"
          disabled={isLoading}
        />
      </form>
      
      {pendingConfirmation && (
        <ConfirmDialog
          data={pendingConfirmation}
          onConfirm={handleConfirm}
          onCancel={() => setPendingConfirmation(null)}
        />
      )}
    </div>
  );
}
```

### 10.4 环境变量配置

```bash
# .env.local

# 数据库
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_KEY=xxx

# AI 模型 API Keys（按需配置）
ANTHROPIC_API_KEY=sk-ant-xxx      # Claude
OPENAI_API_KEY=sk-xxx             # GPT (可选)
GOOGLE_GENERATIVE_AI_API_KEY=xxx  # Gemini (可选)
GROQ_API_KEY=gsk_xxx              # Groq (可选)

# 网站抓取
FIRECRAWL_API_KEY=xxx
```

---

## 11. 总结

### 11.1 核心设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 系统定位 | 版本管理 + 仓储 | 不重复网站功能 |
| 交互模式 | 读用UI，写用对话 | 自然语言更高效 |
| **确认卡片** | **可编辑（内联 + 完整编辑）** | **AI 识别错误时快速修改** |
| 技术栈 | Vite + Vercel Functions | 轻量、部署简单 |
| AI 模型 | 多模型支持 | 灵活切换，优化成本 |
| 设计风格 | 参考 vocab-tracker | 深色默认，骨架屏加载 |
| 响应式 | 移动优先，底部导航栏 | 兼顾桌面和移动端 |
| PWA | 支持离线访问、添加主屏幕 | 移动端体验更好 |
| 位置管理 | 动态学习 + AI 智能匹配 | 自由输入，自动归类 |
| 状态简化 | 工作室/画廊/美术馆/在途/已售 | 更清晰，不过度细分 |
| 币种 | 可选列表 | 支持多币种交易 |
| 认证方式 | 双入口：内部登录 + 画廊链接 | 内部安全，画廊无摩擦 |
| 权限控制 | MVP 用邮箱白名单 | 简单安全，后续可升级 |
| 数据导出 | PDF/MD（带图）+ Excel/CSV + JSON | 覆盖所有使用场景 |
| 数据导入 | MD 文件（eventstructure.com 导出） | 批量导入所有作品 |
| 作品描述 | 不存储 | 通过链接查看网站 |
| 附件类型 | 用户自由描述 | 更灵活 |
| 编号层级 | 版本 | 每个物理实体独立 |

### 11.2 多模型策略

| 任务 | 模型 | 理由 |
|------|------|------|
| 复杂操作 | Claude Sonnet | 工具调用准确 |
| 简单查询 | Gemini Flash | 便宜 |
| 快速响应 | Groq | 极速 |
| 开发测试 | Ollama | 免费 |

### 11.3 成本

**$1-15/月**（取决于模型选择）
- 混合策略推荐：~$1.5/月
- 全用 Claude：~$5/月

### 11.4 时间

**7 周**完成 MVP

---

*文档版本: v3.0*  
*状态: 待确认*
